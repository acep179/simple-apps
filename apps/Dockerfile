# MENENTUKAN IMAGE
FROM node:18.16

# PERINTAH YANG DIJALANKAN
RUN apt update -y && npm install

# COPY FILE TO IMAGE
ADD . /app

# MAINTAINER IMAGE
LABEL email="acep@ilcs.co.id"

# PERINTAH YANG DIJALANKAN PADA SAAT CONTAINER SUDAH UP
COM ["npm", "start"]

# DESCRIBE EXPOSE PORT
EXPOSE 3000